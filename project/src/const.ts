export enum AppRoute {
  SignIn = '/login',
  Favorites = '/favorites',
  Room = '/room',
  Main = '/',
}

export enum AuthorizationStatus {
  Auth = 'AUTH',
  NoAuth = 'NO_AUTH',
  Unknown = 'UNKNOWN',
}
